"""
presentation/ — UI layer (Phase 4).

Existing PyQt5 modules at root (main.py, case_tab.py, reports_tab.py, etc.)
will migrate here during Phase 4. Until then this package is a placeholder.

Layer rules:
  - MAY import from application/ only (not infrastructure/ directly).
  - ViewModels wire application service calls to PyQt5 widgets.
"""
