"""
infrastructure — External dependency implementations (Phase 3).

Layer rules:
  - MAY import from application/ and domain/.
  - Contains concrete repository implementations, encryption, logging.
  - database.py at root remains a legacy shim until Phase 3 is complete.
"""
